How can Pydantic be useful when working with LLM outputs?
It tokenizes the input
It validates and structures model outputs into predefined schemas
It generates prompts for LLMs
It formats responses as CSV
