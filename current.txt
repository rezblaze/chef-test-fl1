import pkg_resources
for pkg in ["langgraph", "langgraph-prebuilt", "langgraph-checkpoint", "langchain", "langchain-core"]:
    try:
        print(pkg, pkg_resources.get_distribution(pkg).version)
    except:
        print(pkg, "not installed")
